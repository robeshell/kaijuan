// Default brand when not using a flavored Xcode configuration.
// Flavored builds use Brand-comic.xcconfig / Brand-book.xcconfig via
// Debug-comic / Debug-book (see tool/apply_xcode_flavors.py).
#include "Brand-comic.xcconfig"
