# 把文档交给 Open Design（双 App）

## 1. 激活设计系统

导入并激活：`docs/opendesign/DESIGN.md`  
（日后可拆 `DESIGN_comic.md` / `DESIGN_book.md`；现阶段一份共享 token + 对话里声明品牌。）

## 2. 附件（推荐）

| 文件 | 用途 |
|------|------|
| `docs/PRODUCT.md` | 双产品功能 |
| `docs/DESIGN_FOUNDATION.md` | 视觉 |
| `docs/ENGINEERING.md` | 可选，工程背景 |
| `docs/opendesign/CONTEXT.md` | 一页摘要 |
| `docs/specs/shelf.md` 等 | 按需 |

## 3. 开场粘贴

```text
Read attached KaikaNext docs. Authority: PRODUCT.md > DESIGN_FOUNDATION.md > specs > DESIGN.md.

Product model:
- TWO separate branded apps (comic + book), NOT one app with 漫画|图书 tabs.
- Shared quiet glass UI language; different names, icons, accents, defaults, import types.
- Data is isolated per app.
- For this task, design the COMIC app only (unless I say book).

Screens (macOS ~1440×900):
1) 书架 continue reading + recent
2) 书库 grid + 导入 + search field
3) 阅读器 page-image #1C1C1E + glass chrome
4) 设置 appearance

Chinese labels. Light shell. No dual-type segments, no cloud/social.
List which docs you used, then generate the prototype.
```

图书稿把 `COMIC` 换成 `BOOK`，阅读器改为纸白正文占位即可。

## 4. 纠正

```text
We ship two apps. Remove any in-app 漫画|图书 primary navigation.
```
